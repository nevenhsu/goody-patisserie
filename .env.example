# Generate with: openssl rand -hex 32
PAYLOAD_SECRET=

# One-time secret required to create the first Payload administrator. Remove
# it from the Worker after the first admin exists.
GOODY_BOOTSTRAP_SECRET=

# Optional local overrides. Production values should be configured as Worker
# secrets/bindings rather than committed to this file.
CLOUDFLARE_ENV=
